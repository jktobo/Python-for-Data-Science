def count_in_list(lst, item):
    """
    Count occurrences of an item in a list.
    Args:
        lst (list): The list to search
        item (any): The item to count
    Returns:
        int: Number of times the item appears in the list
    """
    return lst.count(item)
